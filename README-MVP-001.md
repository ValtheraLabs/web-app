# Valthera MVP-001 Snapshot

This branch creates the first runnable web app shell.

Run locally:

```bash
npm install
npm run dev
```

Main routes:

- `/`
- `/app`
- `/app/swap`
- `/app/portfolio`
- `/app/research`
- `/app/ai`
- `/app/settings`
